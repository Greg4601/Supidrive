import UsersList from "../data/usersList";

export default function Users() {
    return (
        <div>
            <h1 style={{ fontWeight: 900 }}>Users :</h1>
            <UsersList />
        </div>
    );
}